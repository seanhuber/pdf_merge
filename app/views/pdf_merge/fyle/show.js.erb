$('.right-pane').html('<%= j render partial: 'fyle', locals: {fyle: @fyle} %>');
$('.right-pane .select-pages.btn').pageSelector({
  fyle_id: '<%= @fyle.id %>',
  thumbs_url: '<%= fyle_thumbs_path %>',
  page_image_size: '<%= PdfMerge.page_image_size %>',
  setPageSelections: function(event, data) {
    console.log('page selections:', data.selections);
  }
});
